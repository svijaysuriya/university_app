<h4 class="center-align">Course Listing Page</h4>
<div class="row">
	<% @courses.all.each do|i| %>
	<div class="col s12 m6">
		<div class="card blue-grey ">
			<div class="card-content white-text">
				<span class = "card-title"><%= i.short_name %> : <%= i.name %></span>
				<p> <%= i.description %> </p>
				<p><strong >Enrolled Students : </strong> <%= i.enrolled_students %> / <%= i.max_limit %> </p>
			</div>
			<div class="card-action">
			<%= link_to "Info" ,course_path(i)%>
			<% if logged_in? && current_user.admin? %>
				<%= link_to "Edit Course", edit_course_path(i) %>
				<%= link_to "Delete Course", course_path(i), method: :delete, data:{confirm: "Are you sure?",class:"btn btn-outline-danger"}%>
			<%else%>
				<% if logged_in? && (i.enrolled_students < i.max_limit) %>
					<% if !(current_user.courses.include?(i))%>
						<%= link_to "Enroll" ,course_enroll_path(course_id: i.id),method: :post %> 
					<% else %>
						<strong class="green-text text-darken ">Enrolled</strong>
					<% end %>
				<% else %>
					<strong class="green-text text-darken ">enrollments over</strong>
				<%end%>
			<% end %>
			</div>
		</div>
	</div>
	<%end%>
</div>
